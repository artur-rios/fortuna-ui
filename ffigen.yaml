# IR-10, IR-11: the FFI bindings are generated from the Fortuna core's published
# C header, committed, and never hand-edited. A change to the header surfaces as
# a compile error rather than as a runtime crash.
#
# The header is vendored at native/include/fortuna_core.h, copied verbatim from
# the fortuna-api repository. CI diffs the two, so drift fails the build rather
# than being discovered at run time.
#
# Regenerate with:
#   dart run tool/generate_bindings.dart
#
# The header is named for what fortuna-api publishes: its core is a Rust crate
# called `fortuna_core`, so the header is `fortuna_core.h` and the artifacts are
# `libfortuna_core.so` and `fortuna_core.dll`.

name: FortunaBindings
description: |
  Generated bindings over the Fortuna core's C ABI.

  DO NOT EDIT. Generated from native/include/fortuna_core.h by ffigen.
output: 'lib/core/bindings/fortuna_bindings.dart'

headers:
  entry-points:
    - 'native/include/fortuna_core.h'
  include-directives:
    - '**fortuna_core.h'

# The published header documents its route exports in a doc comment that names
# glob patterns like `/api/auth/**`. The `/*` inside `auth/**` is a nested
# comment opener, so clang warns three times, and ffigen refuses on warnings by
# default. The warnings are purely about comment lexing — every declaration
# after them parses normally, and the generated bindings are checked against the
# header's own function list by tool/generate_bindings.dart.
#
# The alternative would be to edit the vendored header, and that is exactly what
# FR-DA-06 forbids: it is vendored verbatim so CI can diff it against the
# published one. The defect belongs to fortuna-api's cbindgen output and is
# fixed there, not here.
ignore-source-errors: true

preamble: |
  // GENERATED — DO NOT EDIT.
  //
  // Produced by `dart run tool/generate_bindings.dart` from the Fortuna core's
  // published C header. A wrong binding means a wrong header: fix it at the
  // source in fortuna-api and regenerate (BR-36).
  //
  // ignore_for_file: type=lint, unused_element, camel_case_types

comments:
  style: any
  length: full
